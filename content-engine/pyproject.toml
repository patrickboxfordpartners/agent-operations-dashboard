[project]
name = "content-engine"
version = "0.1.0"
description = "AI content generation for marketing"
requires-python = ">=3.11"

dependencies = [
    "anthropic>=0.40.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
]
