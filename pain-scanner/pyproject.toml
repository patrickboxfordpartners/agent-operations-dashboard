[project]
name = "pain-scanner"
version = "0.1.0"
description = "Monitors verticals for unsolved problems"
requires-python = ">=3.11"

dependencies = [
    "anthropic>=0.40.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.25.0",
    "feedparser>=6.0.0",
    "praw>=7.7.0",  # Reddit API
]
