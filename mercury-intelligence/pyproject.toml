[project]
name = "mercury-intelligence"
version = "0.1.0"
description = "Real-time banking intelligence for Mercury"
requires-python = ">=3.11"

dependencies = [
    "anthropic>=0.40.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
]

# Optional dependencies
[project.optional-dependencies]
notifications = [
    "slack-sdk>=3.23.0",
]
